<h2>New...</h2>


<div class="block1">
  <div class="new_form">
    <form action="/confirm/<%= params[:splat][0] %>">
        <% session[:fields].each do |x| %>
        <p><%= "#{x}" %>: <input type="text" name="<%= x %>" value="<%= @object.send(x) %>"><p>
        <% end %>
        <% if session[:film] == true && @object.title == "" %>
          <p><input type="checkbox" name="rt" value="yes">Search using RottenTomatoes</p>
        <% end %>
      <input type="submit">
    </form>
  </div>
</div>