<h2>New...</h2>

<div class="block1">
  <div class="new_form">
    <form action="/confirm">
        <% session[:fields].each do |x| %>
        <p><%= "#{x}" %>: <input type="text" name="<%= x %>"><p>
        <% end %>
      <input type="submit">
    </form>
  </div>
</div>