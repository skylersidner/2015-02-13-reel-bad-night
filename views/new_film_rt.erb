<a name="top"></a>
<h2>New...</h2>


<div class="block1">
  <p>RottenTomatoes.com a vast trove of data.  Broad searches can reveal hundreds of results.  You have been warned.</p>
</div>


<div class="block2">
  <form action="/new_film_rt">
    <select name="search_by">
      <option value="title">Title</option>
    </select>
    <input type="text" name="search">
    <input type="submit" value="Search">
  </form>
</div>

<% if @results.count != 0 %>
  <% @results.each do |object| %>
    <form action="/new_film">
      <input type="hidden" name="table" value="<%= @table %>">
      <div class="display_results">
        <% object.instance_variables.each do |attribute| %>
          <% snip = attribute.to_s.delete("@") %>
          <p><%= "#{snip}" %>: <%= object.send(snip) %></p>
          <input type="hidden" name="<%= snip %>" value="<%= object.send(snip) %>">
        <% end %>
        <input type="submit" value="This one!">
      </div>
    </form>
  <% end %>
  <a href="#top">Back to top</a>
<% end %>